node_modules/
.env
data/
uploads/*
!uploads/.gitkeep
bash.exe.stackdump
.claude/

# Motor de OCR (ocr-service/) — ambiente Python
ocr-service/venv/
ocr-service/__pycache__/
ocr-service/**/__pycache__/
# Pacote de idioma do Tesseract (~15MB) — baixado via setup, não versionado
ocr-service/tessdata/*.traineddata
# Saídas geradas pelos scripts de teste (fonte .html/.js fica versionado)
ocr-service/test-fixtures/*.pdf
ocr-service/test-fixtures/*.png

# Arquivos de lock temporários do Word/PowerPoint
~$*.docx
~$*.pptx

# Saída do RPA de simulação do eCAC (executerRPAEcac.py)
declaracoes.json
erro_screenshot.png
__pycache__/

# ATENÇÃO: pasta com o que parece ser um perfil real do Chrome (Login Data,
# History, Sync Data) — nenhum código deste projeto cria/usa essa pasta.
# Nunca deve ir para o Git (vazaria senhas salvas/histórico).
chrome_profile_automacao/
